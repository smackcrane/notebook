import Link from "../src/link.tsx"

# Hello, World!

A website written in Typescript and React; bundled with Vite; MDX smth smth.

$$\LaTeX$$

and plugging into the Boltzmann distribution this becomes

$$
e^{-f(x)/T}
=e^{(\ln(p_1)+\cdots+\ln(p_n))/nT}
=e^{\ln((p_1\cdots p_n)^{1/nT})}
=(p_1\cdots p_n)^{1/nT},
$$

<Link slug="home">Home</Link>
